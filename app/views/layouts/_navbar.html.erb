<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/"><h1 class="text-center"> SOCK IT TO ME</h1></a>
    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav nav-links navbar-right" id="navbar-user-links">
        <% if action_name == "edit" && devise_controller? %>
        <% elsif user_signed_in? %>
          <%= render 'layouts/navbar_dropdown' %>
          <!-- <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <%= current_user.email %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li> <%= link_to "Edit profile", edit_user_registration_path  %></li>
              <li> <%= link_to "View all orders", socks_path %> </li>
              <li> <%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li> -->
        <% elsif resource.errors.empty? %>
          <li> <%= link_to "Log in", new_user_session_path, remote: true, id: "log-in"  %></li>
          <li> <%= link_to "Sign up", new_user_registration_path, remote: true, id: "sign-up"  %></li>
        <% end %>
      </ul>
      <div class="nav navbar-right" id="log-in-form"></div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
